---
title: "math.atan"
kind: function
namespace: math
source: https://www.tradingview.com/pine-script-reference/v6/#fun_math.atan
---

# math.atan

**Category:** Function

## Syntax

_4 overloaded forms:_

```pinescript
math.atan(angle) → simple float
```

```pinescript
math.atan(angle) → input float
```

```pinescript
math.atan(angle) → const float
```

```pinescript
math.atan(angle) → series float
```

## Description

The atan function returns the arctangent (in radians) of number such that tan(atan(y)) = y for any y.

## Arguments

- **`angle`** `simple int/float` — The value, in radians, to use in the calculation.

## Returns

The arc tangent of a value; the returned angle is in the range [-Pi/2, Pi/2].

**Return type(s):** `simple float`
