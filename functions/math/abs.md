---
title: "math.abs"
kind: function
namespace: math
source: https://www.tradingview.com/pine-script-reference/v6/#fun_math.abs
---

# math.abs

**Category:** Function

## Syntax

_8 overloaded forms:_

```pinescript
math.abs(number) → simple int
```

```pinescript
math.abs(number) → input int
```

```pinescript
math.abs(number) → const int
```

```pinescript
math.abs(number) → series int
```

```pinescript
math.abs(number) → simple float
```

```pinescript
math.abs(number) → input float
```

```pinescript
math.abs(number) → const float
```

```pinescript
math.abs(number) → series float
```

## Description

Absolute value of `number` is `number` if `number` >= 0, or -`number` otherwise.

## Arguments

- **`number`** `simple int` — The number to use in the calculation.

## Returns

The absolute value of `number`.

**Return type(s):** `simple int`
